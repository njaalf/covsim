% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/IG.R
\name{calibrate_ig}
\alias{calibrate_ig}
\title{Calibrate the IG (Independent Generator) model}
\usage{
calibrate_ig(sigma.target, skewness, excesskurtosis, typeA = "triang")
}
\arguments{
\item{sigma.target}{Target population covariance matrix.}

\item{skewness}{Target skewness vector.}

\item{excesskurtosis}{Target excess kurtosis vector.}

\item{typeA}{Either \code{"triang"} (default, triangular Cholesky factor) or
\code{"symm"} (symmetric square root).}
}
\value{
An object of class \code{c("covsim_calib_ig", "covsim_calib")} that can
  be passed to \code{\link[stats]{simulate}} to draw samples.
}
\description{
Performs the calibration step of the IG algorithm: computes the transformation
matrix \code{A} and fits a Pearson distribution to each independent generator
such that samples drawn from the calibrated model match the target covariance,
skewness, and excess kurtosis. Calibration is decoupled from simulation so the
same calibration object can be reused to draw many samples without recomputation.
}
\examples{
sigma.target <- diag(3)
cal <- calibrate_ig(sigma.target, skewness = rep(1, 3), excesskurtosis = rep(3, 3))
samples <- stats::simulate(cal, nsim = 5, N = 1000)
}
\references{
Foldnes, N. and Olson, U. H. (2016). A simple simulation technique for nonnormal data with prespecified skewness,
            kurtosis, and covariance matrix. Multivariate behavioral research, 51(2-3), 207-219
}
\seealso{
\code{\link{rIG}}, \code{\link[stats]{simulate}}
}
\author{
Njål Foldnes  (\email{njal.foldnes@gmail.com})
}
