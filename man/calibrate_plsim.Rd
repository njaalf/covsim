% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PLSIM.R
\name{calibrate_plsim}
\alias{calibrate_plsim}
\title{Calibrate the PLSIM (Piecewise Linear) model}
\usage{
calibrate_plsim(
  sigma.target,
  skewness,
  excesskurtosis,
  numsegments = 4,
  gammalist = NULL,
  monot = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{sigma.target}{Target population covariance matrix.}

\item{skewness}{Target skewness vector.}

\item{excesskurtosis}{Target excess kurtosis vector.}

\item{numsegments}{The number of line segments in each marginal.}

\item{gammalist}{A list of breakpoints in each margin.}

\item{monot}{If \code{TRUE}, piecewise linear functions are forced to be monotonous.
The implied copula is then normal.}

\item{verbose}{If \code{TRUE}, prints calibration progress.}
}
\value{
An object of class \code{c("covsim_calib_plsim", "covsim_calib")} that
  can be passed to \code{\link[stats]{simulate}} to draw samples.
}
\description{
Performs the calibration step of the PLSIM algorithm: fits piecewise linear
marginal transforms with the target skewness and kurtosis and searches for
intermediate normal correlations that yield the target covariance after
transformation. The (expensive) pairwise calibration is decoupled from
simulation so the resulting calibration object can be reused to draw many
samples without recomputation.
}
\examples{
set.seed(1)
sigma.target <- cov(MASS::mvrnorm(5, rep(0, 3), diag(3)))
cal <- calibrate_plsim(sigma.target, skewness = rep(1, 3), excesskurtosis = rep(4, 3))
samples <- stats::simulate(cal, nsim = 2, N = 1000)
}
\references{
Foldnes, N. and Grønneberg S. (2021). Non-normal data simulation using piecewise linear transforms.
}
\seealso{
\code{\link{rPLSIM}}, \code{\link[stats]{simulate}}
}
\author{
Njål Foldnes  (\email{njal.foldnes@gmail.com})
}
